---
version: 2
updates:
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "weekly"
    commit-message:
      prefix: "deps"
      include: "scope"
    groups:
      npm-dependencies:
        dependency-type: "production"
      npm-dev-dependencies:
        dependency-type: "development"
